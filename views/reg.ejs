

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
<style>

   
    
.zappy {
  background: rgb(2,0,36);
background: linear-gradient(90deg, rgba(2,0,36,1) 0%, rgba(9,9,121,1) 35%, rgba(0,212,255,1) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
	width: 100%;
  height: fit-content;
	font-size: 40px;
  text-align: center;
  margin-top: 25px;;
  font-family: 'Playfair Display'
 
}
    
    body {
      font-family: Arial, sans-serif;
     bottom: 0;
     background-image: url(./img/contactbanner.jpg);
    }
 
  #userType{
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 4px;
    outline: none;
   
  }

  

  h1 {
    margin-bottom: 20px;
    color: #343a40;
  }

  form {
    text-align: left;
  }

  label {
    display: block;
    margin-bottom: 5px;
    font-weight: bold;
    color: #343a40;
  }

  input {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 4px;
    outline: none;
  }
  .entire{

align-items: center;
justify-content: center;
margin: 0 auto;
margin-top: 100px;;;
width: 90%;
display: flex;
flex-wrap: wrap;
flex-direction: row;
gap: 20%;;
}
.container{
background-color: #ffffff;
    padding: 70px;
    border-radius: 8px;
    box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
    text-align: center;
    width: 300px;

}

  button {
    background-color: #007bff;
    color: #fff;
    padding: 12px 15px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-weight: bold;
    transition: background-color 0.3s;
  }

  button:hover {
    background-color: #0056b3;
  }

  /* a {
    display: block;
    margin-top: 15px;
    color: #007bff;
    text-decoration: none;
    font-size: 14px;
  } */
  .contain{
    float:left;
   
  }
  img{
width: 500px;
height: 550px;;


 

  }
  
@media screen and (max-width: 880px) {
body {
 
  width: 114%;
}
body {
      font-family: Arial, sans-serif;
     bottom: 0;
     background-image: url(./img/bg.jpg);
    }
.container{

    text-align: center;
    width: 900px;

}
.entire{

align-items: center;
justify-content: center;
margin: 0 auto;

width: 90%;
display: flex;
flex-wrap: wrap;
flex-direction: row;

}
.contain{
margin-top: 0;
margin-bottom: 10px;;
}
/* You can add more styles specific for smaller screens here */
}
</style>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
</head>

<body>
  <div class="entire">
 
  <div class="container">
    <h3 class="zappy">Zappy Photography</h3>
    
    <form id="loginForm" action="/register" method="POST">
      
      
        <label for="Username">Username</label>
      <input type="text" id="Username" name="Username" required>
      <label for="Email">Email:</label>
      <input type="email" id="Email" name="Email" required>
      <label for="Password">Password:</label>
      <input type="password" id="Password" name="Password" required onkeyup="checkPasswordStrength(this.value)">
      
      <div id="password-strength"></div>
      
      <button type="submit" id="registerButton" disabled>Register</button>
      
      <script>
      function checkPasswordStrength(password) {
          let strength = 0;
      
          // Length check
          if (password.length >= 8) {
              strength += 1;
          }
      
          // Complexity check
          if (password.match(/[a-z]/) && password.match(/[A-Z]/)) {
              strength += 1;
          }
          if (password.match(/[0-9]/)) {
              strength += 1;
          }
          if (password.match(/[$&+,:;=?@#|'<>.^*()%!-]/)) {
              strength += 1;
          }
      
          // Update password strength text
          let strengthText = '';
          switch (strength) {
              case 0:
              case 1:
                  strengthText = 'Weak';
                  break;
              case 2:
                  strengthText = 'Moderate';
                  break;
              case 3:
              case 4:
                  strengthText = 'Strong';
                  break;
              default:
                  break;
          }
      
          // Update text color based on strength
          let strengthColor = '';
          switch (strength) {
              case 0:
              case 1:
                  strengthColor = 'red'; // Weak password color
                  break;
              case 2:
                  strengthColor = 'orange'; // Moderate password color
                  break;
              case 3:
              case 4:
                  strengthColor = 'green'; // Strong password color
                  document.getElementById('registerButton').disabled = false; // Enable button for strong password
                  break;
              default:
                  break;
          }
      
          // Display password strength with color
          document.getElementById('password-strength').innerText = 'Password Strength: ' + strengthText;
          document.getElementById('password-strength').style.color = strengthColor;
      }
      </script>
      

    </form>
    </div>
  </div>
    </body>
    </html>
